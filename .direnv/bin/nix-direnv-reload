#!/usr/bin/env bash
set -e
if [[ ! -d "/home/raulescobar/Documents/bernardino/typst.ts" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/raulescobar/Documents/bernardino/typst.ts")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/raulescobar/Documents/bernardino/typst.ts" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/raulescobar/Documents/bernardino/typst.ts/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/raulescobar/Documents/bernardino/typst.ts/.envrc" "/home/raulescobar/Documents/bernardino/typst.ts/.direnv"/*.rc
